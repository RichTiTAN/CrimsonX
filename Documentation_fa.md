# CrimsonX - راهنمای جامع کاربر

به راهنمای کاربر **CrimsonX** خوش آمدید! این سند توضیحاتی دقیق در مورد هر ویژگی، تب و تنظیم موجود در برنامه ارائه می‌دهد.

---

## 1. تب خانه
تب خانه داشبورد اصلی CrimsonX است که دسترسی سریع به کنترل‌های اتصال و نظارت لحظه‌ای را به شما می‌دهد.

### 🔴 دکمه اتصال
هسته تپنده برنامه. با کلیک روی این دکمه، پایپ‌لاین اتصال پویا آغاز می‌شود:
1. **استخراج و تست کانفیگ**: CrimsonX به طور نامحسوس کانفیگ‌های تازه را از نودهای ورکر راه دور دریافت می‌کند یا کانفیگ‌های کش‌شده شما را می‌خواند.
2. **بررسی سرعت و تأخیر**: این برنامه این کانفیگ‌ها را در پس‌زمینه آزمایش می‌کند تا از کارکرد آن‌ها مطمئن شود و آن‌ها را بر اساس سرعت رتبه‌بندی می‌کند.
3. **راه‌اندازی موتور**: پس از انتخاب بهترین نودها، موتور Xray-core اجرا می‌شود. اگر در حالت VPN باشید، sing-box نیز برای تسخیر تمام ترافیک سیستم از طریق یک رابط TUN مقداردهی اولیه می‌شود.

### ⚡ پنل تنظیمات سریع
این پنل که در صفحه خانه قرار دارد، دسترسی سریع به گزینه‌های پرکاربرد را بدون نیاز به جستجو در تب تنظیمات فراهم می‌کند. تاگل‌های سریع موجود عبارتند از:
- **UDP مستقیم**
- **نود خروجی Xray**
- **اتصال به آداپتور**
- **آدرس DoH بالادست**
- **DNS سیستم**
- **مسدودکننده تبلیغات و ردیاب**
- **اجازه اتصالات LAN**
- **اجرا با ویندوز**
- **اتصال خودکار**
- **شروع کوچکشده**
- **کوچک کردن به tray**
- **مستثنی کردن قارهها**
- **کانفیگهای دلخواه**
- **غیرفعال کردن بررسی پسزمینه**
- **غیرفعال کردن تعویض یکپارچه**

### 🔄 حالت‌های عملیاتی
روی متن زیر دکمه اتصال کلیک کنید تا بین حالت‌های عملیاتی جابجا شوید:
- **حالت VPN**: ۱۰۰٪ ترافیک اینترنت رایانه شما را با استفاده از یک آداپتور شبکه مجازی (TUN) تسخیر می‌کند. بهترین گزینه برای گیمینگ یا برنامه‌هایی که تنظیمات پروکسی را رعایت نمی‌کنند.
- **حالت پروکسی**: تنظیمات پروکسی سیستم ویندوز را تغییر می‌دهد. فقط برنامه‌هایی که از پروکسی سیستم پیروی می‌کنند (مانند مرورگرهای وب) از طریق CrimsonX مسیریابی می‌شوند.
- **حالت بدون پروکسی**: تنظیمات پروکسی سیستم ویندوز را غیرفعال می‌کند و به سیستم شما اجازه می‌دهد اتصال مستقیمی به اینترنت داشته باشد، در حالی که هنوز پورت پروکسی را در پس‌زمینه برای برنامه‌هایی که در آن‌ها تنظیمات پروکسی را به صورت دستی پیکربندی می‌کنید، باز می‌گذارد.

---

## 2. پوشش‌های آمار و گزارشات
از طریق آیکون‌های ناوبری کوچک در داشبورد اصلی قابل دسترسی است.

### 📊 نمای آمار
تله‌متری لحظه‌ای از اتصال فعال شما را ارائه می‌دهد:
- **سرعت‌ها**: نمودارهای زنده سرعت آپلود و دانلود و بازخوانی متن.
- **مصرف داده**: کل مقدار پهنای باندی که در طول جلسه فعلی مصرف شده است.
- **تأخیر (پینگ)**: تأخیر لحظه‌ای به سرور راه دور.
- **اطلاعات شبکه**: کشور سروری که به آن متصل هستید، **پورت لوکال** شما و **IP شبکه محلی** (در صورت فعال بودن اشتراک‌گذاری LAN) را نمایش می‌دهد. *(توجه: IP عمومی شما عمداً در اینجا نمایش داده نمی‌شود).*

### 📝 نمای گزارشات
یک پنجره شبیه به ترمینال که گزارشات خام را مستقیماً از موتور اصلی Xray خروجی می‌دهد. برای دیباگ کردن مشکلات اتصال یا تأیید اینکه قوانین مسیریابی خاصی در حال فعال شدن هستند، مفید است.

---

## 3. تب اسپلیت تانل
اسپلیت تانل به شما کنترل دقیقی روی اینکه چه ترافیکی از طریق پروکسی مسیریابی شود و چه ترافیکی از اتصال اینترنت مستقیم و عادی شما استفاده کند، می‌دهد.

### حالت‌های مسیریابی
- **غیرفعال**: اسپلیت تانل خاموش است. تمام ترافیک با توجه به حالت عملیاتی اصلی شما مسیریابی می‌شود.
- **اختصاصی**: فقط برای برنامهها، دامنهها، IPها و پورتهای لیست شده پروکسی را *دور بزنید*. هر چیز دیگری از طریق پروکسی عبور می‌کند.
- **شامل**: *فقط* برنامهها، دامنهها، IPها و پورتهای لیست شده را از طریق پروکسی مسیریابی کنید. هر چیز دیگری از اینترنت مستقیم شما استفاده می‌کند.

### دسته‌بندی‌های اسپلیت
- **برنامهها**: روی "افزودن" کلیک کنید تا یک فایل اجرایی (مثلاً `chrome.exe`) را مرور و انتخاب کنید. برنامه‌های انتخاب شده از قانون مسیریابی اختصاصی یا شامل انتخابی شما پیروی می‌کنند.
- **دامنهها، IPها و پورتها**: دامنه‌های خاص (مثلاً `example.com`)، آدرس‌های IP یا پورت‌هایی را که می‌خواهید مسیریابی یا دور بزنید، وارد کنید.
- **دامنهها، IPها و پورتهای مسدود شده**: دامنه‌ها، IPها یا پورت‌هایی را که می‌خواهید به طور کامل از دسترسی به اینترنت مسدود کنید، وارد کنید.

### UDP مستقیم
- **تاگل UDP مستقیم**: تمام ترافیک UDP (مانند صدای Discord یا بازی‌های رقابتی) را مجبور می‌کند پروکسی را دور زده و از اتصال اینترنت مستقیم شما استفاده کند، که تضمین‌کننده حداقل تأخیر در حالی است که ترافیک TCP پراکسی شده باقی می‌ماند.

---

## 4. تب تنظیمات
قلب سفارشی‌سازی CrimsonX، که به بخش‌های خاصی تقسیم شده است:

### اجرا
- **اجرا با ویندوز**: هنگامی که وارد ویندوز می‌شوید، به طور خودکار CrimsonX را راه‌اندازی می‌کند.
- **اتصال خودکار**: به محض شروع برنامه، به طور خودکار دنباله اتصال را آغاز می‌کند.
- **شروع کوچکشده**: برنامه را به جای باز کردن پنجره اصلی، بی‌صدا در پس‌زمینه باز می‌کند.
- **کوچک کردن به tray**: هنگام کلیک روی دکمه بستن `X` یا کوچک کردن برنامه، به جای بسته شدن کامل، در سینی سیستم (نزدیک ساعت) پنهان می‌شود.

### اتصال
- **مستثنی کردن قارهها**: قاره‌های خاصی (مانند آسیا، اروپا) را انتخاب کنید. کانفیگ‌های واقع در این قاره‌ها به طور کامل از تست سرعت و اتصالات مستثنی خواهند شد.
- **کانفیگهای دلخواه**: تا دو مورد از رشته‌های VLESS/VMESS/Shadowsocks خصوصی خود را وارد کنید.
  - **اجازه اتصال با یک کانفیگ**: اگر تیک خورده باشد، حتی اگر فقط یکی از دو کانفیگ سفارشی شما کار کند، برنامه با موفقیت متصل خواهد شد.
- **نود خروجی Xray**: یک فایل `.json` را وارد کنید یا یک لینک اشتراک‌گذاری (مانند `vless://`، `vmess://`) حاوی پیکربندی سفارشی Xray را قرار دهید تا به عنوان نود خروجی عمل کند.
- **اتصال به آداپتور**: تمام ترافیک پروکسی را مجبور می‌کند منحصراً از طریق آداپتور شبکه خاصی که از منوی کشویی انتخاب می‌کنید، خارج شود.
- **تنظیمات DNS**:
  - **آدرس DoH بالادست**: به جای پرس‌وجوهای متن ساده، DNS را از طریق DNS-over-HTTPS (DoH) رمزگذاری شده حل کنید، که نشت DNS و سانسور را کاهش می‌دهد.
  - **DNS سیستم**: هنگام اتصال، DNS اول (PRIMARY DNS) و DNS دوم (SECONDARY DNS) سیستم خود را مجبور به استفاده از آدرس‌های IPv4 خاصی کنید.
- **اجازه اتصالات LAN**: پورت پروکسی را روی شبکه محلی شما باز می‌کند و به تلفن، کنسول یا تلویزیون هوشمند شما اجازه می‌دهد به IP رایانه شخصی شما متصل شده و VPN را به اشتراک بگذارند.
  - **احراز هویت**: از دستگاه‌های موجود در شبکه می‌خواهد تا برای استفاده از پروکسی LAN شما یک نام کاربری و رمز عبور ارائه دهند.
- **مسدودکننده تبلیغات و ردیاب**: درخواست‌ها به دامنه‌های شناخته شده تبلیغات و ردیاب را قبل از خروج از رایانه شخصی شما حذف می‌کند. Xray دامنه‌های منطبق را به یک خروجی blackhole مسیریابی می‌کند.
- **سیاست تعادل بار**: نحوه توزیع اتصالات Xray در سراسر نودهای پروکسی شما را کنترل می‌کند:
  - `چرخشی`: به طور متوالی در میان نودها می‌چرخد.
  - `کمبارترین`: ترافیک را به نودی ارسال می‌کند که کمترین اتصالات فعال را دارد.
  - `کمترین پینگ`: نودی که سریع‌ترین زمان پاسخ‌گویی را পাশخ‌گویی را دارد، در اولویت قرار می‌دهد.
  - `تصادفی`: با انتخاب یک نود تصادفی، از نظر آماری ترافیک را متعادل می‌کند.

### سیستم
- **غیرفعال کردن بررسی پسزمینه**: برنامه را از تست مداوم کانفیگ‌های جدید در پس‌زمینه در حالی که متصل هستید، متوقف می‌کند.
- **غیرفعال کردن تعویض یکپارچه**: از واکشی کانفیگ‌های جدید هر ۱ ساعت و جایگزینی یکپارچه آن‌ها جلوگیری می‌کند.
- **زبان**: تغییر کل رابط کاربری بین انگلیسی و فارسی (`فارسی`). دارای پشتیبانی کامل از چینش راست‌به‌چپ (RTL) است.
- **حالت دیباگ**: ثبت خطاهای دقیق را فعال می‌کند. برای خواندن گزارشات در `error.log` زمانی که چیزی موفق به اتصال نمی‌شود، مفید است.
- **میانبر دسکتاپ**: دکمه‌ای با یک کلیک برای ایجاد میانبر CrimsonX روی دسکتاپ شما.
- **میانبر منوی استارت**: دکمه‌ای با یک کلیک برای ایجاد میانبر CrimsonX در منوی استارت شما.

---

## 5. تب تم ها
به شما امکان می‌دهد زیبایی بصری برنامه را شخصی‌سازی کنید.
- بین تم های گرادیانت پویا یکی را انتخاب کنید: زرشکی، آبی، بنفش، سبز، صورتی و زرد.
- تم انتخاب شده بلافاصله دکمه تپنده اتصال، حاشیه‌های ناوبری، کلیدهای تغییر وضعیت و رنگ‌های پس‌زمینه را به‌روزرسانی می‌کند.

---

## 6. تب درباره ما
- نسخه نصب شده فعلی CrimsonX را نمایش می‌دهد.
- به طور خودکار به‌روزرسانی‌ها را بررسی می‌کند و دانلود OTA (Over-The-Air) و استخراج نسخه‌های جدید از GitHub را انجام می‌دهد.
- لینک‌های سریع به مخزن GitHub پروژه و کانال‌های تلگرام جامعه را ارائه می‌دهد.
